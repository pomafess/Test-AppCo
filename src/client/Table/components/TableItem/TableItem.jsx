import React from 'react';
import PropTypes from 'prop-types';
import s from './TableItem.module.scss';

const TableItem = ({ restProps }) => {
    
    return 
}

TableItem.defaultProps = {
   
};

TableItem.propTypes = {
   
}

export default TableItem;
